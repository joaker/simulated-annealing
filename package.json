{
  "name": "simulated-annealing",
  "version": "1.0.0",
  "description": "use the simulated annealing heuristic to solve np-complete problems in polynomial time",
  "main": "index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json && tsc -p tsconfig-cjs.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "isomorphic-timers-promises": "^1.0.1"
  },
  "devDependencies": {
    "typescript": "^4.9.5"
  }
}
